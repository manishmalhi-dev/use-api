import 'package:flutter/material.dart';

class TabletsCategory extends StatelessWidget {
  const TabletsCategory({super.key});

  @override
  Widget build(BuildContext context) {
    return Expanded(
      child: Column(
        children: [
          Text("Tablets page "),
        ],
      ),
    );
  }
}
